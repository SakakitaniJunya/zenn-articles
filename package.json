{
  "name": "zenn-articles",
  "version": "1.0.0",
  "private": true,
  "description": "Junya Sakakitani / CreaNest — AI 駆動開発の記録 (Zenn). Drafts are generated by Claude Code (CLI), not by Anthropic API directly.",
  "scripts": {
    "new:article": "zenn new:article",
    "new:book": "zenn new:book",
    "preview": "zenn preview",
    "draft": "bash scripts/generate-draft.sh",
    "draft:id": "bash scripts/generate-draft.sh",
    "publish:next": "bash scripts/publish-next.sh"
  },
  "devDependencies": {
    "zenn-cli": "^0.1.158"
  }
}
